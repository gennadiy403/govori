#!/bin/bash
cd "$(dirname "$0")"
# Load secrets (ANTHROPIC_API_KEY etc.) if present
[ -f "$HOME/.config/govori/env" ] && source "$HOME/.config/govori/env"
source .venv/bin/activate
python govori.py "$@"
